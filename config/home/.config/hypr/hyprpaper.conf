# GrgOS — hyprpaper (wallpaper daemon)
# The wallpaper is generated at build/install time to an absolute, user-agnostic
# path so this file works for every user without edits.
preload = /usr/share/grgos/wallpaper.png
wallpaper = , /usr/share/grgos/wallpaper.png
splash = false
ipc = on
